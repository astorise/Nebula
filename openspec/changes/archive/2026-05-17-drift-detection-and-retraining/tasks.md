# Implementation Tasks

- [x] `task-1`: Create the `nebula-drift-monitor` FaaS crate.
- [x] `task-2`: Implement the sliding window algorithm and metric aggregation using `tachyon:store/kv`.
- [x] `task-3`: Define the threshold logic and emit `nebula.drift.detected` events with contextual payloads.
- [x] `task-4`: Update `nebula-curriculum-generator` to subscribe to drift events and generate targeted learning material.
- [x] `task-5`: Update the VSCode Dashboard (in `packages/extension/src/webview/components/DriftPanel.tsx`) to visualize current drift metrics and recent automated retraining triggers.
