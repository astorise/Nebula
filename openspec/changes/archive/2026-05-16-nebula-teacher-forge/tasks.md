# Implementation Tasks

- [x] `task-1`: Add the Rust workspaces `nebula-curriculum-generator`, `nebula-teacher-arbitrator`, `nebula-dataset-forge`, and `nebula-training-orchestrator` under `faas/`.
- [x] `task-2`: Implement the curriculum generator with a JSON schema and injection of correlated tasks into Tachyon.
- [x] `task-3`: Implement the Tier 3 arbitrator with micro-batching, layer-by-layer orchestration, and constrained JSON decoding.
- [x] `task-4`: Implement the dataset forge with the 60/40 ratio, append-only JSONL persistence, and the `nebula.training.ready` signal.
- [x] `task-5`: Implement the LoRA orchestrator with training, merge, OCI publication through `wkg`, and UI notification.
