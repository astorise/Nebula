# Implementation Tasks

- [x] `task-1`: Update `nebula-dataset-forge` to handle and persist the new `{"prompt", "chosen", "rejected"}` JSONL schema.
- [x] `task-2`: Create `nebula-dpo-judge` to inject constitutional rules into the Tier 3 arbitration requests.
- [x] `task-3`: Implement the DPO loss function and sequential forward-pass logic in the `nebula-training-orchestrator` using Candle.
- [x] `task-4`: Develop the `AlignmentPanel.tsx` in the VSCode extension to manage the Constitution KV store.
- [x] `task-5`: Implement the "Preference Reviewer" UI allowing manual curation of the DPO dataset via WebSocket.
