# Implementation Tasks

- [x] `task-1`: Implement the Luhn algorithm in `nebula-data-anonymizer` and write negative tests proving non-Luhn numbers are ignored.
- [x] `task-2`: Update `build-rootfs.sh` in `nebula-wasm-foundry` to calculate and append `sha256sum` to the artifact metadata.
- [x] `task-3`: Write the `verify_openspec.sh` script and integrate it into `.github/workflows/ci.yml`.
- [x] `task-4`: Do a full codebase sweep to add `// spec: ...` comments to existing unit and integration tests.
