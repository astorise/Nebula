# Implementation Tasks

- [x] `task-1`: Create the `nebula-quantization-engine` Wasm crate.
- [x] `task-2`: Implement the Candle-based Q8_0 and Q4_K conversion logic for `.safetensors` files.
- [x] `task-3`: Update the `training-orchestrator` to generate OCI Image Indexes mapping artifact variants to `min-vram` requirements.
- [x] `task-4`: Update the `system-faas-config-api` (in the CLI) to parse and present these OCI annotations to the frontend.
- [x] `task-5`: Implement the "Artifact Size Table" in the VSCode `DeploymentPanel.tsx`.
