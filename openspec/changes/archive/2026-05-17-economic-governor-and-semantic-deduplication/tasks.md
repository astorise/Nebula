# Implementation Tasks

- [x] `task-1`: Create the `nebula-semantic-deduplicator` Wasm crate using Candle for local embeddings and cosine similarity math.
- [x] `task-2`: Create the `nebula-economic-governor` Wasm crate using `tiktoken-rs` for fast token estimation.
- [x] `task-3`: Refactor `nebula-teacher-arbitrator` to report exact API token usage back to the event bus for reconciliation.
- [x] `task-4`: Build the `FinOpsPanel.tsx` in the VS Code extension to visualize budgets and deduplication savings.
- [x] `task-5`: Create E2E tests simulating a flood of identical failing queries to verify that only the first query triggers an external API call.
