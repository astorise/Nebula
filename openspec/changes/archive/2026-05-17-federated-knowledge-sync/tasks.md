# Implementation Tasks

- [x] `task-1`: Create the `nebula-p2p-sync-agent` Wasm crate. Implement the Bloom filter generation and Tachyon Gossip API bindings.
- [x] `task-2`: Create the `nebula-crdt-merger` Wasm crate. Implement the deterministic SHA-256 row hashing and deduplication logic.
- [x] `task-3`: Update the `nebula-dataset-forge` to write index keys into `tachyon:store/kv` alongside the volume writes to support fast delta lookups.
- [x] `task-4`: Implement the `FederationPanel` in the VSCode extension to visualize peer nodes and data provenance.
- [x] `task-5`: Add WebSocket events to stream the active P2P sync status up to the VSCode UI.
