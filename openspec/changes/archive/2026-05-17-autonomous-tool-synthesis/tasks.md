# Implementation Tasks

- [x] `task-1`: Implement the `nebula-tool-architect` FaaS to generate Rust/WIT compliant source code via the Tier 3 model.
- [x] `task-2`: Create the `nebula-wasm-foundry` MicroVM rootfs builder containing the `cargo-component` toolchain.
- [x] `task-3`: Implement the VSOCK gRPC server within the foundry to handle remote compilation requests.
- [x] `task-4`: Update the `dataset-forge` to handle Tool Calling SFT/DPO formats (JSON schema injection).
- [x] `task-5`: Build the `FoundryPanel.tsx` in VSCode for Human-in-the-Loop code review of synthesized tools.
