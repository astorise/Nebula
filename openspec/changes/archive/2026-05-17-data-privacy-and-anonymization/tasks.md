# Implementation Tasks

- [x] `task-1`: Create the `nebula-data-anonymizer` Wasm crate using the `regex` library for fast pattern matching.
- [x] `task-2`: Implement the default dictionary of compliance rules (Tokens, PII, Financial data) within the anonymizer.
- [x] `task-3`: Refactor `nebula-telemetry-gateway` to consume the downstream sanitized topic instead of the raw edge stream.
- [x] `task-4`: Develop the `PrivacyPanel.tsx` component in the VSCode extension.
- [x] `task-5`: Wire the "Live Test Sandbox" in the UI to the CLI engine to allow developers to validate their custom Regex rules securely.
